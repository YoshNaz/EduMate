<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduMate</title>
    <style>
        body {
            text-align: center;
            font-family: Arial, sans-serif;
            margin: 50px;
        }
        .logo img {
            width: 150px; /* Adjust size for logo */
        }
        .container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        .card {
            border: 2px solid #000;
            padding: 20px;
            width: 200px;
            text-align: center;
            border-radius: 10px;
        }
        button {
            margin-top: 10px;
            padding: 10px;
            border: none;
            background-color: #007BFF;
            color: white;
            cursor: pointer;
            border-radius: 5px;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="logo">
        <img src="logo.png" alt="EduMate Logo"> <!-- Replace with logo file afer image upload to file in github -->
    </div>
    <h1>EduMate</h1>
    <div class="container">
        <div class="card">
            <h3>NOTES</h3>
            <button id="notesBtn">Generate Notes</button>
        </div>
        <div class="card">
            <h3>FLASHCARD</h3>
            <button id="flashcardBtn">Generate Flashcards</button>
        </div>
        <div class="card">
            <h3>QUIZZES</h3>
            <button id="quizBtn">Generate Quizzes</button>
        </div>
    </div>
    <script>
        document.getElementById("notesBtn").addEventListener("click", function () {
            window.location.href = "notes.html"; //put html file name for notes
        });
        document.getElementById("flashcardBtn").addEventListener("click", function () {
            window.location.href = "flashcard.html"; //put html file name for flashcard
        });
        document.getElementById("quizBtn").addEventListener("click", function () {
            window.location.href = "quiz.html"; //put html file name for quiz
        });
    </script>
</body>
</html>
